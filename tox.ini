[tox]
envlist = py39
skipsdist = True
 
[testenv]
deps =
    aiokafka
    coverage
    kafka-python
    nats-py
    pytest
    pyyaml

commands =
    coverage run -m pytest
    coverage xml
 
[coverage:run]
relative_files = True
source = ./telematic_system/
branch = True
